extends ../layout

block content
    .card
        .card-header
            h1= 'Hello ' + user + '!'
        .card-body 
            -var journal = 'http://localhost:3000/journal/pages/' + user;
            a(class="btn btn-secondary" href=journal) Journal